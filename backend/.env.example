MSSQL_SA_PASSWORD=Your_strong_password123!
# Independent from MSSQL_SA_PASSWORD above — pick a separate password for the Seq admin account.
SEQ_ADMIN_PASSWORD=Another_strong_password456!

# Used by the api container (docker-compose.yml) — Production values only, never commit real ones.
JWT_SIGNING_KEY=change-me-to-a-long-random-string
IYZICO_API_KEY=
IYZICO_SECRET_KEY=
SMTP_HOST=
SMTP_USERNAME=
SMTP_PASSWORD=
